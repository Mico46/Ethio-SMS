export { db, auth, storage, default as app } from './firebase';
export { FirestoreProvider, useFirestore, FirestoreContext } from './FirestoreContext';
export { seedDatabase, SCHOOL_ID } from './seedData';
